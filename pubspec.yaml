name: face_auth_compatible
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Core Flutter packages
  cupertino_icons: ^1.0.8
  
  # Firebase - Latest stable versions
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.17.5
  firebase_storage: ^11.6.5
  firebase_messaging: ^14.7.10
  cloud_functions: ^4.7.6
  
  # Face Authentication
  flutter_face_api: ^4.1.1
  
  # Media & Camera
  image_picker: ^1.0.7
  video_player: ^2.8.2
  
  # Storage & Data
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  sqflite: ^2.3.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2
  path: ^1.9.0
  
  # Location & Maps
  geolocator: ^10.1.0
  google_maps_flutter: ^2.5.3
  geocoding: ^2.1.1
  
  # Utilities
  uuid: ^4.3.3
  intl: ^0.19.0
  connectivity_plus: ^5.0.2
  permission_handler: ^11.2.0
  device_info_plus: ^10.1.0
  url_launcher: ^6.2.4
  
  # File Picker - Updated to latest version
  file_picker: ^8.0.0+1
  
  # UI & Graphics
  flutter_svg: ^2.0.9
  flutter_local_notifications: ^17.0.0
  
  # HTTP & Network
  http: ^1.2.0
  dio: ^5.4.0
  
  # State Management
  flutter_bloc: ^8.1.3
  get_it: ^7.6.7
  
  # Geometry & Math
  polylabel: ^1.0.1
  geodesy: ^0.4.0
  
  # Audio - Updated for better iOS compatibility
  audioplayers: ^6.0.0
  
  # ML Kit - Updated version
  google_mlkit_face_detection: ^0.10.0
  
  # Development
  mockito: ^5.4.4
  build_runner: ^2.4.8

dev_dependencies: 
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# ✅ REMOVED: Dependency overrides that were causing conflicts
# The file_picker_platform_interface override was causing the version solving to fail

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/
